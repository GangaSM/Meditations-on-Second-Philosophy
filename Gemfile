# frozen_string_literal: true

source "https://rubygems.org"

# Mirrors the exact gem versions GitHub Pages builds with.
gem "github-pages", group: :jekyll_plugins

# Ruby 3+ no longer ships a webserver; needed for `bundle exec jekyll serve`.
gem "webrick", "~> 1.8"
